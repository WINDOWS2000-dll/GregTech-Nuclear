/**
 * Put your {@code @Mixin}-annotated classes directly in this package (not a sub-package) -- it's the one
 * buildscript.properties' {@code mixinsPackage} points at. This file exists only so the package itself is
 * tracked by git (an empty directory isn't) and so the build's "does mixinsPackage resolve to a real
 * directory" check passes before you've added your first Mixin.
 */
package com.windows2000.gtnuclear.mixin;
